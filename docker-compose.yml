services:
  database:
    image: postgres:17.5
    environment:
      - POSTGRES_DB=practice-todo
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - 5432:5432

volumes:
  postgres_data: